spring:
  application:
    name: eurekaserver
server:
  port: 4399
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:4399/eureka/
